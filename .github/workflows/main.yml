name: First Workflow

on:
  push:
    branches:
      - main


jobs:
  example-job:
    runs-on: ubuntu-latest
    
    steps:
      - name: Print a welcome message
        run: echo "Welcome to GitHub Action Course!"


      - name: Checkout repository
        uses: actions/checkout@v5.0.0

      - name: list repository files
        run: ls -la


      
